<%= form_with model: @monologue, local: true, url: new_or_edit_path do |form| %>
<% if @monologue.errors.any? %>
	<ul class="error-list">
		<% @monologue.errors.full_messages.each do |msg| %>
			<li class="alert alert-danger col-xs-offset-1 col-xs-9"><%= msg %></li>
		<% end %>
	</ul>
<% end %>

		<%= form.label :content, "comment:", class: 'col-xs-offset-2 col-xs-12 text-left form-label' %>
		<%= form.text_area :content, size: '80×5', class: 'col-xs-8 col-xs-offset-2 form-texarea' %>
	<%= form.submit new_or_edit_submit_message, class: 'btn btn-warning  col-xs-offset-3 col-xs-6 form-submit-btn' %>
<% end %>